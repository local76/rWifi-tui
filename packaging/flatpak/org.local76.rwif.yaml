app-id: org.local76.rwif
runtime: org.freedesktop.Platform
runtime-version: '23.08'
sdk: org.freedesktop.Sdk
command: rwif
modules:
  - name: rwif
    buildsystem: simple
    build-commands:
      - cargo build --release --locked
      - install -Dm755 target/release/rwif /app/bin/rwif
